using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum EPlayer
{
    CREATE,
    WALK,
    RUN,
    JUMP,
    LAND,
    ATTACK,
    DIE,
    RESET
}

public enum EPlayerAnim
{
    WALK = 0,
    RUN = 1,
    JUMP = 2,
    LAND = 3,
    ATTACK = 4,
    DIE = 5,
}
